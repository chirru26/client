'use client'

import Link from 'next/link'
import { ArrowLeft, ArrowUpRight, Github, FolderGit2, Code2, Layers } from 'lucide-react'
import { motion } from 'framer-motion'
import { findProjectBySlug, toProjectSlug } from '@/utils/slugUtils'
import { getImageUrl } from '@/utils/imageUtils'
import type { Project } from '@/types/portfolio'

export default function ProjectDetailsPage({projects=[],slug}:{projects?:Project[];slug:string}){
 const project=findProjectBySlug(projects,slug)
 if(!project)return <main style={{paddingTop:'120px',minHeight:'80vh'}}><div className="container" style={{textAlign:'center',maxWidth:'600px'}}><h1 style={{fontSize:'2rem',marginBottom:'16px',color:'var(--text-primary)'}}>Project Not Found</h1><p style={{color:'var(--text-secondary)',marginBottom:'24px'}}>We couldn't locate any project matching &quot;{slug}&quot;.</p><Link href="/projects" className="btn btn-primary"><ArrowLeft size={16}/> Back to Projects</Link></div></main>
 const cleanDesc=project.description&&project.description.length>6&&project.description!=='dfsb'?project.description:'A modern full-stack application developed with Java, Spring Boot, and reactive architecture.'
 return <main style={{paddingTop:'100px',minHeight:'85vh',paddingBottom:'80px'}}><div className="container" style={{maxWidth:'900px'}}><nav aria-label="Breadcrumb" style={{display:'flex',alignItems:'center',gap:'8px',marginBottom:'28px',fontSize:'.88rem'}}><Link href="/" className="project-action-link">Home</Link><span style={{color:'var(--text-muted)'}}>/</span><Link href="/projects" className="project-action-link">Projects</Link><span style={{color:'var(--text-muted)'}}>/</span><span style={{color:'var(--text-secondary)'}}>{project.title}</span></nav><motion.article initial={{opacity:0,y:20}} animate={{opacity:1,y:0}} transition={{duration:.4}} className="hero-card" style={{padding:'36px 32px'}}><div style={{display:'flex',flexWrap:'wrap',alignItems:'center',justifyContent:'space-between',gap:'16px',marginBottom:'20px'}}><div><span className="eyebrow" style={{display:'inline-flex',alignItems:'center',gap:'6px',marginBottom:'8px'}}><FolderGit2 size={14}/> Software Project</span><h1 style={{fontSize:'2.2rem',fontWeight:800,color:'var(--text-primary)',lineHeight:1.2}}>{project.title}</h1></div>{project.featured&&<span className="project-featured-badge" style={{position:'static'}}>Featured Project</span>}</div>{project.imageUrl&&<div style={{borderRadius:'var(--radius-xl)',overflow:'hidden',margin:'20px 0 32px',border:'1px solid var(--border-medium)',background:'var(--bg-surface-container)'}}><img src={getImageUrl(project.imageUrl)||undefined} alt={`Screenshot and overview of ${project.title} project`} style={{width:'100%',maxHeight:'480px',objectFit:'cover',display:'block'}} loading="eager" decoding="async"/></div>}<section style={{marginBottom:'32px'}}><h2 style={{fontSize:'1.35rem',fontWeight:700,marginBottom:'12px',color:'var(--text-primary)',display:'flex',alignItems:'center',gap:'8px'}}><Layers size={18} color="var(--primary)"/> Overview</h2><p style={{color:'var(--text-secondary)',fontSize:'1.05rem',lineHeight:1.7,whiteSpace:'pre-line'}}>{cleanDesc}</p></section>{project.skills&&project.skills.length>0&&<section style={{marginBottom:'32px'}}><h2 style={{fontSize:'1.35rem',fontWeight:700,marginBottom:'14px',color:'var(--text-primary)',display:'flex',alignItems:'center',gap:'8px'}}><Code2 size={18} color="var(--primary)"/> Technology Stack</h2><div style={{display:'flex',flexWrap:'wrap',gap:'10px'}}>{project.skills.map(skill=><span key={skill.id||skill.name} className="tech-chip" style={{fontSize:'.88rem',padding:'6px 14px'}}>{skill.name}</span>)}</div></section>}<section style={{paddingTop:'20px',borderTop:'1px solid var(--border-subtle)'}}><h2 style={{fontSize:'1.2rem',fontWeight:700,marginBottom:'16px',color:'var(--text-primary)'}}>Project Links</h2><div style={{display:'flex',flexWrap:'wrap',gap:'14px'}}>{project.liveUrl&&<a href={project.liveUrl} target="_blank" rel="noreferrer" className="btn btn-primary"><ArrowUpRight size={16}/> View Live Demo</a>}{project.githubUrl&&<a href={project.githubUrl} target="_blank" rel="noreferrer" className="btn btn-secondary"><Github size={16}/> View Source on GitHub</a>}<Link href="/projects" className="btn btn-ghost"><ArrowLeft size={16}/> All Projects</Link></div></section></motion.article></div></main>
}
